{
  "name": "@openui_hub/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "openui_hub 平台使用端（组件瀑布流卡片 + 远程渲染预览；管理员可删除/发布）",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "type-check": "vue-tsc --build"
  },
  "dependencies": {
    "@openui_hub/runtime": "workspace:*"
  },
  "devDependencies": {
    "@tsconfig/node24": "^24.0.4",
    "@vue/tsconfig": "^0.9.1"
  },
  "engines": {
    "node": "^22.18.0 || >=24.12.0"
  }
}

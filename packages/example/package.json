{
  "name": "@openui_hub/example",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "openui_hub 独立示例工程（使用者视角，验证构建期插件通道）",
  "scripts": {
    "dev": "vite",
    "build": "vite build"
  },
  "dependencies": {
    "@openui_hub/cli": "workspace:*",
    "@openui_hub/plugin_vite": "workspace:*",
    "@openui_hub/runtime": "workspace:*"
  },
  "engines": {
    "node": "^22.18.0 || >=24.12.0"
  }
}